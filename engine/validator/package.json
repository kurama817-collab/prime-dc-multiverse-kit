{
  "name": "@prime-dc/validator",
  "version": "0.1.0",
  "private": true,
  "description": "Validator for canon event schemas and doctrine checks.",
  "main": "dist/validate-events.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'no lint configured'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
